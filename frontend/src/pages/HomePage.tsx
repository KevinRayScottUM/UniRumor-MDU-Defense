export function HomePage() {
  return (
    <section aria-labelledby="home-heading" className="page-panel">
      <p className="page-eyebrow">Verification workspace</p>
      <h1 id="home-heading">Check a focal claim against a video</h1>
      <p>
        Submission controls will be added in the next workflow phase. This
        foundation communicates only with the public backend API.
      </p>
    </section>
  );
}

